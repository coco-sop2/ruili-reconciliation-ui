# 文件说明：CherryStudio agent 接口相关环境变量示例。
# Leave the URL empty to keep the UI in local empty-data mode.
#
# The start button posts multipart/form-data here, then CherryStudio can call
# the configured agent skill.
VITE_CHERRYSTUDIO_AGENT_URL=
VITE_CHERRYSTUDIO_AGENT_SKILL=reconciliation.start
